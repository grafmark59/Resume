import React from "react"

export default ({ children }) =>
  <div
    style={
      {
        width: '100%',
        height: '100%',
        margin: 'auto',
        backgroundColor: 'rgb(255, 255, 255)'
      }
    }>
    {children}
  </div>
